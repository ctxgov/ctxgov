---
id: public_review_long_noise
title: Synthetic public long noisy decoy review note
source_url: synthetic://ctxvault/v0.3.3-public-review/long-noisy-decoy
public_data_basis: Synthetic public fixture for budget and decoy-resistance checks
---

# Long noisy public note

This fixture intentionally repeats many release-review words so the suite can
test whether the package remains readable when public data is larger than the
owner wants to inject. It mentions privacy, release readiness, onboarding,
local search, connector boundary, receipts, public review, token budget,
projection, API output, and logs, but it should not be treated as the best
answer to narrow marker-based questions.

# Budget boundary

The over-budget boundary uses this source because it is large enough to exceed
a deliberately tiny budget. The expected behavior is not a ready handoff. The
expected behavior is a clear over-budget warning, an auditable receipt, and a
selection status that tells the owner to reduce selected context or raise the
budget before projecting.

# Decoy paragraph

This paragraph is intentionally verbose. It describes local package review,
source selection, human approval, privacy preflight, deterministic fixtures,
release gates, context windows, public data, synthetic logs, API-shaped
responses, CLI onboarding, README review, receipt inspection, projection
hashes, source refs, selected slice refs, and safe handoff claims. It is useful
as background noise, but it should not beat exact targeted markers in the
small-source scenario.

Public review marker: budgettrimreview budgetoverrunreview.
