# Project Notes

The report should show candidate memory/context refs, omitted refs, blocked
refs, stale or superseded refs, imported context refs, and missing proof before
remembered state is allowed to influence behavior.

Deletion path: if a local note is removed, derived reports under `.ctxvault/`
must be regenerated before reuse.

